<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recognize_coordinate"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.rakaadinugroho.sealmood.core.activities.RecognizeActivity">
    <ImageView
        android:id="@+id/recognize_image"
        android:scaleType="centerCrop"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" />
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/recognize_create"
        android:layout_margin="@dimen/big_margin"
        android:layout_gravity="center|bottom"
        android:src="@drawable/ic_photo_camera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:id="@+id/recognize_no_data"
        android:text="@string/no_take_picture"
        android:textColor="@color/colorMutedFull"
        android:textSize="18sp"
        android:layout_gravity="center_horizontal|center_vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</android.support.design.widget.CoordinatorLayout>
